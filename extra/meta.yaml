package:
  name: packagename
  version: versionofpackage

source:
  git_rev: gitversion
  git_url: https://github.com/repositoryname/packagename.git

requirements:
  build:
    - python
    - setuptools
    - numpy

  run:
    - python
    - numpy
    - wxpython

    
test:
  imports:
    - packagename

about:
  home: https://github.com/repositoryname/packagename
  license: GPL3
  license_file: LICENSE
  description: 'The package displays ...'
